<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khacchung.makevideo">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <service
            android:name=".service.CreateVideoService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.PlayVideoActivity" />
        <activity android:name=".activity.CutSoundActivity" />
        <activity android:name=".activity.CreatedFileActivity" />
        <activity android:name=".activity.SaveVideoActivity" />
        <activity android:name=".activity.CreateVideoActivity" />
        <activity android:name=".activity.MoveIndexActivity" />
        <activity android:name=".activity.SelectImageActivity" />
        <activity android:name=".activity.ShowImageFullScreenActivity" />
        <activity android:name=".activity.ListImageEditActivity" />
        <activity android:name=".activity.PermissionActivity" />
        <activity android:name=".activity.EditImageActivity" />
        <activity android:name=".activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>